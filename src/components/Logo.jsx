import React from 'react';
import styles from './Logo.module.css';

const Logo = () => (
  <div className={styles.logoBox}>
    <h1 className={styles.logoText}>NFL Stats Champion</h1>
  </div>
)

export default Logo;